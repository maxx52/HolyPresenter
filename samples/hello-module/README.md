# Hello Module Sample

This module demonstrates how to create a simple HolyPresenter module using only `platform-api`.

## What it shows

- Implementing `HolyModule`
- Defining `ModuleMetadata`
- Receiving `ModuleContext`
- Rendering a simple Workspace UI

## Build

```bash
./gradlew :samples:hello-module:build