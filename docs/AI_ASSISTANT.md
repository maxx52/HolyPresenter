# ИИ-помощник HolyPresenter

Встроенный модуль создаёт текст, изображения и короткие видео, показывает
предварительную стоимость и не выполняет запрос, если он превысит месячный
лимит пользователя.

## Бесплатный режим

Без регистрации и API-ключа доступны четыре бесплатных режима:

- **Бесплатные шаблоны** — работают сразу, полностью офлайн и почти не создают
  нагрузки на слабый компьютер;
- **Локальная нейросеть** — Qwen3 1.7B через Ollama, работает на компьютере
  пользователя без оплаты и передачи текста облачному провайдеру. Qwen3
  создаёт только текст;
- **Бесплатные фоны** — встроенный генератор цветных PNG-фонов, работает без
  нейросети, интернета и видеокарты;
- **Локальная нейросеть ComfyUI** — настоящая генерация изображений на
  компьютере пользователя без оплаты API.

Для локальной нейросети выберите «Текст» → «Локальная нейросеть». HolyPresenter
проверит Ollama и предложит:

1. скачать Ollama с официального сайта;
2. запустить Ollama;
3. нажать «Проверить снова»;
4. нажать «Установить модель» — будет загружена `qwen3:1.7b` размером около
   1,4 ГБ.

На старых ПК генерация текста может занимать заметное время. Qwen3 автоматически
выгружается из памяти после короткого простоя.

### Локальные изображения через ComfyUI

1. В ИИ-помощнике выберите «Изображение» → «Локальная нейросеть ComfyUI».
2. Установите и запустите ComfyUI Desktop.
3. Установите в ComfyUI любую совместимую checkpoint-модель. Для первого запуска
   подойдёт модель SD 1.5 из официальной инструкции ComfyUI.
4. Нажмите «Проверить снова». HolyPresenter автоматически выберет найденный
   checkpoint и покажет его название.
5. Введите запрос и нажмите «Создать».

HolyPresenter обращается только к локальному адресу `127.0.0.1:8188`, отправляет
стандартный workflow ComfyUI и сохраняет результат в `ai/generated`. ComfyUI
запускается отдельно и не расходует память, когда выключена. Для хорошей скорости
рекомендуется дискретная видеокарта; на слабых ПК следует выбрать встроенные
«Бесплатные фоны».

Официальные страницы:

- https://ollama.com/download
- https://ollama.com/library/qwen3:1.7b
- https://docs.ollama.com/api/chat
- https://www.comfy.org/download
- https://docs.comfy.org/get_started/first_generation
- https://docs.comfy.org/development/comfyui-server/api-examples

## Экономные настройки по умолчанию

- текст: `gpt-5.6-luna`, не более 1200 выходных токенов;
- изображения: `gpt-image-2`, 1536×1024, среднее качество;
- видео: `sora-2`, 1280×720, 4 секунды;
- месячный лимит: 10 USD.

Перед каждым запросом HolyPresenter показывает оценку и требует подтверждение.
Фактическая стоимость успешного запроса записывается в
`settings/ai-usage.json`. Пользователь может увеличить лимит и выбрать более
мощную текстовую модель в расширенных настройках.

Лимит является локальной защитой HolyPresenter на конкретном компьютере. Для
жёсткого ограничения всего аккаунта дополнительно настройте бюджет проекта в
панели OpenAI.

## API-ключ

Каждый пользователь добавляет собственный ключ OpenAI. На Windows ключ
шифруется DPAPI для текущей учётной записи и хранится в
`cloud/openai-api-key.dpapi`. Каталог `cloud` исключён из резервных копий, поэтому
ключ не переносится на другой компьютер. Также поддерживается переменная среды
`OPENAI_API_KEY`.

Ключ нельзя добавлять в исходный код, JAR, MSI или журнал приложения.

В карточке персонального ключа доступны официальные страницы OpenAI:

- [получить API-ключ](https://platform.openai.com/api-keys);
- [настроить оплату API](https://platform.openai.com/settings/organization/billing/overview).

Подписка ChatGPT Plus и использование API оплачиваются отдельно.

## Расширение провайдерами

`AiProvider` и `AiProviderRegistry` находятся в `platform-api`. Внешний модуль
может реализовать собственный провайдер и зарегистрировать его при включении:

```kotlin
override fun onEnable(context: ModuleContext) {
    context.services
        .get(AiProviderRegistry::class)
        ?.register(MyAiProvider())
}
```

Провайдер обязан заранее оценивать стоимость, выполнять тяжёлую работу вне UI
потока и возвращать фактически списанную либо наиболее точную известную
стоимость.

## Ограничение видео

OpenAI объявила Videos API и Sora 2 устаревшими с отключением 24 сентября 2026
года. Поэтому видеогенерация отделена интерфейсом `AiProvider`: новый провайдер
можно подключить без изменения интерфейса ИИ-помощника и планировщика.

Официальная документация:

- https://developers.openai.com/api/docs/pricing
- https://developers.openai.com/api/docs/guides/image-generation
- https://developers.openai.com/api/docs/guides/video-generation
